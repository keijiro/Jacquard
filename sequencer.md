# シーケンサ仕様メモ

`bp.html` はシーケンス画面のモックアップである。静的な見た目のみを実装しており、
音を出す機能も編集機能も持たない。このメモはコードを読んでも分からない部分、
つまり各要素が何を意味するのか、なぜその形なのか、何がまだ決まっていないのかを
補うためのものである。

## 基本モデル

グリッドは 1 枚の平面で、そこに **レーン** を置く。レーンは横方向に伸びる
ステップの列で、**横軸が時間**、1 列が 1 ステップに対応する。

レーンは平面上の任意の位置から始められる。開始位置はグリッド座標で持ち、
レーン同士が同じ座標を占めることはない。現在のモックアップには 2 本のレーンがある。

- メインレーン — 16 ステップ
- バリエーションレーン — 6 ステップ。メインレーンの下、横位置もずらして配置

1 ステップは縦方向に最大 4 つのスロットを持ち、**同時に起こる事柄を縦に積む**。
和音の構成音も、そのステップに掛かる制御要素も、すべてこの縦のスタックに入る。

### 縦チェーンの規則 — 上が支配、下が従属

縦に隣接するセルは 1px の実線で繋がれ、ひとつの塊として読ませる。
この連結には向きの意味がある。

**上のセルが下のセルを従える。**

- `NTRG` / `PROB`（発火条件）が上にあり、その下にノートがぶら下がる
  → 上の条件が、下のノートの発火を許すかどうかを決める
- ノートが上にあり、その下に `PABS` / `PREL` / `PDLT` がぶら下がる
  → 下のパラメーターロックが、上のノートに付随する属性として働く

一見すると効き方が逆に見えるが、どちらも「上のものが下のものを従える」で一貫している。
結果としてスタックは上から順に **発火条件 → ノート → パラメーター** の並びになる。

この規則には既知の弱点がある。連結線はすべて同一の 1px 線なので、
`PROB:4 / A4 / PABS` のような列を見たとき、**どちらの向きに結合しているかは
トークンの種類を知らないと読めない**。制御要素の背景を灰色にして種類の判別を
容易にしたのは、この弱点の緩和策である。

### レールと端点

各レーンは点線が水平に貫く。これがそのレーンの時間軸で、両端を白塗り潰しの
セルが挟む。

**単純に右へ進む以外の挙動をする端点は、すべて白塗り潰しのセルとする。**
これを規則とした。現在 4 種類ある。

| トークン | アイコン | 挙動 |
| --- | --- | --- |
| `CH1` | テキスト | シーケンスの開始点 |
| `TERM` | 左へ U ターンする矢印 | 終端。先頭へ戻る |
| `JUMP` | 角の丸い Z 字＋右矢印 | 別レーンへ飛ぶ |
| `JDST` | 縦線＋右矢印 | 飛び先。レーンの開始 |

`TERM` は最終ステップの右隣に自動的に置かれる。かつて点線レールは「最後の ▶
マーカー」を終点にしていたが、これは最終ステップの最上段にノートが入ると
レールが手前で切れる実装だった。終端を暗黙に推測させず、明示的なセルとして
持たせることで解消している。

`▶` マーカーは、そのステップの最上段（スロット 0）が空のときに現れる。
結果として ▶ は「何も置かれていない拍」だけに並び、素通りする区間の目印として働く。
これは意図して設計したものではなく実装から生じた振る舞いだが、
読みやすさに寄与しているため現状維持としている。

### 3 種類の線

線の種類が意味を担っている。

| 見た目 | 意味 |
| --- | --- |
| 1px 白の実線（縦） | 構造的な従属。同一ステップ内のスタックを繋ぐ |
| 2px 白の点線（横） | レーンの時間軸。左から右へ流れる |
| 1px 灰の実線（角丸、軸をずらす） | レーンをまたぐジャンプの経路 |

ジャンプの線だけはセル中心を通らず、列中心の右 7.5px / 行中心の下 7.5px を通る。
格子の `・` を潰さずに横断させるためで、結果としてレールとは別のレイヤに乗って
いるように読める。

`JUMP` のセルは、この 3 種類のうち 2 種類が出会う唯一の場所である。上からは
ゲートの白実線（従属関係）が、下へは灰の実線（時間的な飛び先）が伸びる。

## トークン

### ノート

`C4` / `F#4` / `Ab4` のように音名＋オクターブで書き、`C4/4` のように `/` の後に
長さを付けられる。長さを省略した場合の既定値と、長さの単位（ステップ数か拍か）は
**未定**。臨時記号はセル内で上付き表示になる。

### パラメーターロック

ノートの下にぶら下げて、そのノートのパラメーターを操作する。
アイコンはいずれもフェーダーを基本形とし、右側の修飾子で種類を分ける。

| トークン | アイコン | 意味 |
| --- | --- | --- |
| `PABS` | フェーダー単体 | 絶対値でパラメーターを設定する |
| `PREL` | フェーダー＋ ↕ | そのステップにおいてのみ相対的に変更する |
| `PDLT` | フェーダー＋ Δ | 相対的な変更を永続的に適用する。パラメーターを上げ続ける／下げ続けるといった用途 |

`PREL` と `PDLT` の違いはループをまたいだときに現れる。`PREL` はそのステップを
通過するたびに同じ相対量を適用するだけで元の値は変わらないが、`PDLT` は変更が
累積していく。

**どのパラメーターを対象とするのか、変化量がいくらなのかは、まだ表現手段を持たない。**
現在のアイコンは「パラメーターロックである」ことしか示しておらず、対象と値の
指定方法は未設計である。ここは今後の主要な検討事項。

### 発火条件

ノートや `JUMP` の上に置いて、下のセルが発火するかどうかを制御する。

| トークン | アイコン | 意味 |
| --- | --- | --- |
| `NTRGn:m` | n 個の矩形、m 番目が塗り | ループの特定の周のみ発火する。`NTRG4:3` は 4 周のうち 3 周目 |
| `PROB:n` | 円グラフ | 指定された確率でランダムに発火する |

`PROB` の値はモックアップでは 12 分割の円グラフとして描かれるため `n/12` として
扱っている（`PROB:4` は 4/12）。この 12 分割という粒度が仕様として妥当かは未検討で、
単に時計状のアイコンが描きやすい分割数だったという以上の根拠はない。

`NTRG` は n が大きいと表示が破綻する。矩形列の幅は `n * 7 - 1` px なので、
n=5 でちょうどセル幅 34px と等しくなって余白が消え、n=6 以上ははみ出す。
「8 周に 1 回」のような指定は現在の表示方式では表現できない。

## 分岐シーケンス

`JUMP` セルに到達すると、次のステップは自分のレーンの右隣ではなく、
接続先レーンの先頭になる。接続先は必ず `JDST` セルである。

`JUMP` は**単体ではほぼ意味を持たない**。無条件に飛ぶだけなら、
2 本のレーンを 1 本に繋いで書くのと変わらない。

意味を持つのは **発火条件と組み合わせたとき**である。`JUMP` の上に条件を置くと、
条件が成立した周だけ飛び、成立しなければそのまま自分のレーンを右へ進む。

- `NTRG` と組み合わせる → 定期的にシーケンスを別のバリエーションへ変化させる
- `PROB` と組み合わせる → ランダムにシーケンスを変化させる

モックアップでは `NTRG4:4` ＋ `JUMP` を 10 番目のステップに置いている。
4 周に 1 回だけ、そこからバリエーションレーンの 6 ステップへ飛ぶ。
メイン 10 ステップ ＋ バリエーション 6 ステップ = 16 ステップで、
どちらの経路も同じ長さになるよう置いた。

条件を付けるためには `JUMP` がスタックの 2 段目以降に来る必要がある
（1 段目はレール行で、その上に条件を置けない）。したがって **`JUMP` を
レール行に直接置くと、条件を付けられない = 無条件ジャンプにしかならない。**

## 決定済みの割り切り

### 和音の構成音は個別のパラメーターを持てない

1 ステップは線形のスタックなので、`F4 / Ab4 / C5` の和音のうち `Ab4` だけに
パラメーターロックを付ける、という表現ができない。スタックの下にロックを置くと
直上の構成音に付いたようにしか見えない。

これは **和音全体に同一のパラメーターが適用される** という仕様として割り切る。
構成音ごとのロックは提供しない。

代替案として「付随セルを横に置く」「従属セルを半セル分インデントしてツリーにする」も
検討したが、前者は横軸＝時間という約束を壊し、後者はグリッドの規則性を失うため採用しなかった。

## 未決事項

パラメーター関連

- パラメーターロックの**対象パラメーターと変化量**の表現方法（最優先）
- ノートの長さの単位と既定値
- `PROB` の確率の粒度、`NTRG` の n の上限

分岐シーケンス関連

- 飛び先レーンの `TERM` はどこへ戻るのか。シーケンス全体の先頭か、
  飛び出したレーンの先頭か、飛び出した位置の次か
- 飛び先レーンの中にさらに `JUMP` を置けるか（多段分岐）
- 1 本のレーンに複数の `JUMP` を置けるか。複数の `JUMP` が同じ `JDST` を
  指してよいか
- `JDST` を持たないレーン、あるいはどこからも飛ばれない `JDST` を許すか

全体

- 1 ステップあたりのスロット数。現在は 4 だが、仕様上の上限なのか
  モックアップの都合なのか未確定
- レーンとチャンネルの関係。レーンは同一チャンネル内の経路の分岐であり、
  `CH1` が示唆する複数チャンネルとは別の概念だが、2 本目のチャンネルを
  どう並べるかは未設計
- テンポ、拍子、ステップの時間解像度
- 編集操作。セルにホバー効果は付いているが、選択・入力・削除の操作モデルは未定

## モックアップの実装メモ

仕様ではなく `bp.html` 固有の事情。

- `lanes` の各要素が 1 本のレーン。`x` / `y` が開始位置（グリッド絶対座標）、
  `steps[ステップ][スロット]` が内容、`head` が先頭セル（`CH1` または `JDST`）。
  `TERM` は最終ステップの右隣に自動で置かれる
- `links` の各要素が 1 本のジャンプ。接続先はレーン番号で指定し、実際の座標は
  そのレーンの `head` 位置から導出する。**座標を直接書けないので、`JUMP` が
  `JDST` 以外に接続することが構造的に起こり得ない**
- 値の先頭に `*` を付けると白塗り潰しのセルになる
- アイコンは 15×15 の viewBox で、線幅 1px、座標は半整数に揃えてある。
  1px ストロークの中心を画素境界に乗せて滲みを防ぐためで、`bits()` / `pie()` の
  線幅とも一致させている。ジャンプ線のオフセットが 7.5px という半端な値なのも同じ理由
- 描画関数の名前は形状に由来する（`fader` / `delta` / `chevron` / `uTurn` /
  `zigzag` / `bits` / `pie`）。`bits()` は `NTRG` を、`pie()` は `PROB` を描くが、
  トークン名との対応は取っていない
- Δ は左辺と底辺を 1px、右辺を 1.6px にした活字風の意匠。各辺を自身の太さ分だけ
  内側にオフセットして内三角形を求め、外形との差分を `evenodd` で塗っている。
  太さの異なるストロークを重ねる方式では頂点にコブが出るため採用しなかった
- ジャンプ線は SVG レイヤ（`.links`）に描く。角を丸めるため。`roundedPath()` が
  点列から角丸パスを生成する。レールの点線は今も div の
  `repeating-linear-gradient` で描いており、2 つの方式が混在している
