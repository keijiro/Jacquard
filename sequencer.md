# シーケンサ仕様メモ

`bp.html` はシーケンス画面のモックアップである。静的な見た目のみを実装しており、
音を出す機能も編集機能も持たない。このメモはコードを読んでも分からない部分、
つまり各要素が何を意味するのか、なぜその形なのか、何がまだ決まっていないのかを
補うためのものである。

## 基本モデル

グリッドの **横軸は時間**、1 列が 1 ステップに対応する。現在は 16 ステップ。

1 ステップは縦方向に最大 4 つのスロットを持ち、**同時に起こる事柄を縦に積む**。
和音の構成音も、そのステップに掛かる制御要素も、すべてこの縦のスタックに入る。

### 縦チェーンの規則 — 上が支配、下が従属

縦に隣接するセルは 1px の連結線で繋がれ、ひとつの塊として読ませる。
この連結には向きの意味がある。

**上のセルが下のセルを従える。**

- `NTRG` / `PROB`（制御要素）が上にあり、その下にノートがぶら下がる
  → 上の条件が、下のノートの発火を許すかどうかを決める
- ノートが上にあり、その下に `PABS` / `PREL` / `PDLT` がぶら下がる
  → 下のパラメーターロックが、上のノートに付随する属性として働く

一見すると効き方が逆に見えるが、どちらも「上のものが下のものを従える」で一貫している。
結果としてスタックは上から順に **発火条件 → ノート → パラメーター** の並びになる。

この規則には既知の弱点がある。連結線はすべて同一の 1px 線なので、
`PROB:4 / A4 / PABS` のような列を見たとき、**どちらの向きに結合しているかは
トークンの種類を知らないと読めない**。制御要素の背景を灰色にして種類の判別を
容易にしたのは、この弱点の緩和策である。

### レール（時間軸）

y=1 の行を点線が水平に貫く。これがトラックの時間軸で、両端を塗り潰しのセルが挟む。

- 左端 `CH1` — チャンネル。ループの始点
- 右端 `TERM` — 終端。左に U ターンする矢印で、ループが先頭に戻ることを示す

`TERM` は 16 ステップの右隣（`endX = offX + steps.length`）に置かれる。
かつて点線は「最後の ▶ マーカー」を終点にしていたが、これは最終ステップの最上段に
ノートが入るとレールが手前で切れる実装だった。終端を暗黙に推測させず、
明示的なセルとして持たせることで解消している。

`▶` マーカーは、そのステップの最上段（スロット 0）が空のときに現れる。
結果として ▶ は「何も置かれていない拍」だけに並び、素通りする区間の目印として働く。
これは意図して設計したものではなく実装から生じた振る舞いだが、
読みやすさに寄与しているため現状維持としている。

## トークン

### ノート

`C4` / `F#4` / `Ab4` のように音名＋オクターブで書き、`C4/4` のように `/` の後に
長さを付けられる。長さを省略した場合の既定値と、長さの単位（ステップ数か拍か）は
**未定**。臨時記号はセル内で上付き表示になる。

### パラメーターロック

ノートの下にぶら下げて、そのノートのパラメーターを操作する。
アイコンはいずれもフェーダーを基本形とし、右側の修飾子で種類を分ける。

| トークン | アイコン | 意味 |
| --- | --- | --- |
| `PABS` | フェーダー単体 | 絶対値でパラメーターを設定する |
| `PREL` | フェーダー＋ ↕ | そのステップにおいてのみ相対的に変更する |
| `PDLT` | フェーダー＋ Δ | 相対的な変更を永続的に適用する。パラメーターを上げ続ける／下げ続けるといった用途 |

`PREL` と `PDLT` の違いはループをまたいだときに現れる。`PREL` はそのステップを
通過するたびに同じ相対量を適用するだけで元の値は変わらないが、`PDLT` は変更が
累積していく。

**どのパラメーターを対象とするのか、変化量がいくらなのかは、まだ表現手段を持たない。**
現在のアイコンは「パラメーターロックである」ことしか示しておらず、対象と値の
指定方法は未設計である。ここは今後の主要な検討事項。

### 発火条件

ノートの上に置いて、下のノート群が鳴るかどうかを制御する。

| トークン | アイコン | 意味 |
| --- | --- | --- |
| `NTRGn:m` | n 個の矩形、m 番目が塗り | ループの特定の周のみ発火する。`NTRG4:3` は 4 周のうち 3 周目 |
| `PROB:n` | 円グラフ | 指定された確率でランダムに発火する |

`PROB` の値はモックアップでは 12 分割の円グラフとして描かれるため `n/12` として
扱っている（`PROB:4` は 4/12）。この 12 分割という粒度が仕様として妥当かは未検討で、
単に時計状のアイコンが描きやすい分割数だったという以上の根拠はない。

## 決定済みの割り切り

### 和音の構成音は個別のパラメーターを持てない

1 ステップは線形のスタックなので、`F4 / Ab4 / C5` の和音のうち `Ab4` だけに
パラメーターロックを付ける、という表現ができない。スタックの下にロックを置くと
直上の構成音に付いたようにしか見えない。

これは **和音全体に同一のパラメーターが適用される** という仕様として割り切る。
構成音ごとのロックは提供しない。

代替案として「付随セルを横に置く」「従属セルを半セル分インデントしてツリーにする」も
検討したが、前者は横軸＝時間という約束を壊し、後者はグリッドの規則性を失うため採用しなかった。

## 未決事項

- パラメーターロックの**対象パラメーターと変化量**の表現方法（最優先）
- ノートの長さの単位と既定値
- `PROB` の確率の粒度
- 1 ステップあたりのスロット数。現在は 4（`stepRows`）だが、仕様上の上限なのか
  モックアップの都合なのか未確定
- 複数チャンネル／複数トラックの扱い。`CH1` という名前はチャンネルの存在を示唆するが、
  2 本目以降をどう並べるかは未設計
- テンポ、拍子、ステップの時間解像度
- 編集操作。セルにホバー効果は付いているが、選択・入力・削除の操作モデルは未定

## モックアップの実装メモ

仕様ではなく `bp.html` 固有の事情。

- データは `steps[ステップ][スロット]` の二次元配列。空文字列や欠損は空セル
- `extras` はグリッド絶対座標をキーに取る別テーブルで、レールの `CH1` / `TERM` を置く。
  ステップ座標系とは別系統なので、トラックヘッダを増やすなら整理が必要
- 値の先頭に `*` を付けると白塗り潰しのセルになる（レールの両端に使用）
- アイコンは 15×15 の viewBox で、線幅 1px、座標は半整数に揃えてある。
  1px ストロークの中心を画素境界に乗せて滲みを防ぐためで、`bits()` / `pie()` の
  線幅とも一致させている
- 描画関数の名前は形状に由来する（`fader` / `delta` / `chevron` / `uTurn` / `bits` / `pie`）。
  `bits()` は `NTRG` を、`pie()` は `PROB` を描くが、トークン名との対応は取っていない
- Δ は左辺と底辺を 1px、右辺を 1.6px にした活字風の意匠。各辺を自身の太さ分だけ
  内側にオフセットして内三角形を求め、外形との差分を `evenodd` で塗っている。
  太さの異なるストロークを重ねる方式では頂点にコブが出るため採用しなかった
